## Goal
Implement all instructor pages (placeholders + missing nav pages + profile), wire the dashboard to real data, create the missing `/api/instructor/*` routes, and fix the type/schema mismatches by extending `schema.prisma` (per your decision) so all Prisma types line up with the code.

## Part A — Schema extension + migration (source of truth fix)
The codebase already assumes these fields (payout-service, live-classes API/pages, earnings API). Reconcile by **adding** them to `prisma/schema.prisma`:

- **`LiveClass`**: add `subject String?`, `maxParticipants Int @default(100)`, `currentParticipants Int @default(0)`, `recordingUrl String? @db.Text`, `courseId String?` (+ `course Course?` relation), `meetingLink String?`, `updatedAt DateTime @updatedAt`. (Keep existing `meetingUrl`.)
- **`Course`**: add back-relations `liveClasses LiveClass[]` and `transactions Transaction[]`.
- **`Transaction`**: add `courseId String?` (+ `course Course?` relation), `instructorPayout Int?`, `description String?` (`platformFee`/`revenueSplit` already exist).
- **`Instructor`**: add `pendingEarnings Int @default(0)` (next to existing `totalEarnings`).
- **`Payout`**: add `paymentMethod String?`, `processedAt DateTime?`, `metadata Json?`, `createdAt DateTime @default(now())` (keep existing `paidAt`).

Then generate the artifacts (no DB connection needed for `generate`):
1. `npx prisma migrate dev --name instructor_live_earnings_fields --create-only` → writes the migration SQL without applying.
   - Fallback if it complains about DB: hand-write `prisma/migrations/<ts>_instructor_live_earnings_fields/migration.sql`.
2. `npx prisma generate` → regenerates the client so all Prisma types resolve. (Types will be correct either way.)

## Part B — New service file (matches codebase pattern)
Create **`src/lib/instructor/instructor-service.ts`** (`InstructorService` + exported singleton), mirroring `course-service` / `forum-service` / `payout-service`. Methods:
- `resolveByUserId(userId)` → returns the `Instructor` row (used everywhere to convert session user id → instructor id).
- `getDashboardStats(instructorId)` — courses/active students/earnings/rating/pending.
- `getAnalytics(instructorId, range?)` — enrollments over time, revenue by course, ratings distribution, top courses (CSS/SVG-friendly shapes).
- `getProfile(instructorId)`, `updateProfile(instructorId, data)` — bio/expertise/bankDetails.
- `getEarningsSummary(instructorId)` — reuses Prisma + extended fields; returns summary, payouts, recent transactions, monthly series.

## Part C — Pages in `src/app/(instructor)/instructor/`
All server components query prisma/services directly (the existing student-page pattern); client forms POST to API routes. Replace placeholders & add missing:

1. **`dashboard/page.tsx`** — replace hardcoded mock with `instructorService.getDashboardStats`; keep current UI, add working links to Create Course / Schedule Live Class.
2. **`courses/page.tsx`** (replace placeholder) — `courseService.getInstructorCourses`, status filter, stats, empty state, links to new/detail.
3. **`courses/[courseId]/page.tsx`** (replace placeholder) — `courseService.getCourseById`, ownership guard, modules/enrollments/reviews, actions (submit for review, archive) via small client component.
4. **`live-classes/page.tsx`** (NEW) — list instructor's classes grouped scheduled/live/ended, "Schedule" button; uses extended fields.
5. **`live-classes/new/page.tsx`** (NEW, client) — schedule form POSTing to `/api/instructor/live-classes`.
6. **`live-classes/[classId]/start/page.tsx`** — light fix to use `meetingLink`/`subject` safely (already functional; make field access schema-correct).
7. **`earnings/page.tsx`** (replace placeholder) — summary cards, payout history table, recent transactions, monthly series, request-payout client component.
8. **`analytics/page.tsx`** (replace placeholder) — revenue trend (SVG line), revenue-by-course bars, rating distribution, top courses table. No new deps.
9. **`community/page.tsx`** (NEW) — reuse `forumService.getForums` / `getThreads` + "New Discussion" link (instructors participate in the same community).
10. **`settings/page.tsx`** (NEW, client) — bio/expertise/bank details + notification preferences (modeled on student `settings` page).
11. **`profile/page.tsx`** (replace placeholder) — instructor public-ish profile: bio, expertise, verified badge, course/student/rating counts, bank-details-present indicator, edit link.

Also add `error.tsx`/`loading.tsx`/`layout.tsx` only where the page is substantial and a sibling pattern exists (courses/analytics/earnings/profile already have them; I'll add minimal ones for live-classes/new and community/settings if missing — matching the existing 3-file convention).

## Part D — API routes under `src/app/api/instructor/`
(Only `/instructor/earnings` exists today, and it's broken.) Create:
- **`courses/route.ts`** — `GET` (instructor's courses) + `POST` (create; resolves userId→instructor.id and calls `courseService.createCourse` correctly — fixes the latent bug where `/api/courses` passes the *user* id as instructorId). Wire `courses/new/page.tsx` to POST here.
- **`courses/[courseId]/route.ts`** — `GET`/`PUT`/`DELETE`(archive) + `PATCH` submit-for-review, with ownership checks.
- **`live-classes/route.ts`** — `GET` (instructor's) + `POST` (schedule, extended-schema fields).
- **`live-classes/[classId]/route.ts`** — `GET`/`PUT`(update status/recording)/`DELETE`.
- **`earnings/route.ts`** — **rewrite** existing `GET` + `POST` (request payout) to use the extended schema fields (`pendingEarnings`, `instructorPayout`, `Payout.paymentMethod/processedAt/createdAt/metadata`); reuse `payoutService` where it fits.
- **`analytics/route.ts`** — `GET` aggregates.
- **`profile/route.ts`** — `GET`/`PUT`.
- **`settings/route.ts`** — `GET`/`PUT` (account + notification preferences).
- **`community/route.ts`** — thin `GET`/`POST` wrapper around `forumService` (instructor-side convenience, mirrors `/api/community`).

All routes follow the existing pattern: `getServerSession(authOptions)` → require `INSTRUCTOR` → resolve instructor profile → service/prisma call → `NextResponse.json({ success, data })`, with try/catch → 500.

## Part E — Verification
- `npx prisma generate` succeeds (types resolve).
- `npx tsc --noEmit` (or `pnpm build`/`next lint`) to confirm no type errors across new files and the rewritten earnings route.
- Spot-check that referenced fields (`subject`, `recordingUrl`, `instructorPayout`, `pendingEarnings`, etc.) exist on the generated client.

## Notes / out of scope
- I will **not** broadly refactor the student live-classes pages or `/api/live-classes` (non-instructor); the schema extension makes their existing field references valid, which resolves the type errors there as a side benefit. I'll only touch instructor-owned files.
- No new npm dependencies (analytics use inline SVG/CSS).
- I won't run the dev server/DB; verification is via `prisma generate` + typecheck/build.